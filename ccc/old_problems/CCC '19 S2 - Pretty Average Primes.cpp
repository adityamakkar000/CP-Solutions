#include <iostream>
#include <bits/stdc++.h>
#include <cctype>
#include <math.h>
#include <string.h>
#include <stdio.h>
#include <string>
#include <iostream>
#include <iomanip>

using namespace std;

# define ll long long

int main(){

    int a,b,c; cin >> a >> b >> c;
    if(a + b + c - max(a,b,c) > max(a,b,c){
        cout << "Triangle ecists" << endl;
    } else{
        cout << "Triangle does not exist" << endl;
    }
}
